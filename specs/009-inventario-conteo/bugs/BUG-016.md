---
name: BUG-016-flujo-orden-invertido
description: Flujo de Iniciar Conteo tiene orden conceptual invertido - consultar items DESPUÉS de crear inventario en lugar de ANTES
metadata:
  type: project
  severity: Critical
  status: Patched
  date: 2026-07-18
  patched_date: 2026-07-18
---

# BUG-016: Flujo de Iniciar Conteo Tiene Orden Invertido

**Type**: Design inversion (architectural logic error)  
**Severity**: Critical  
**Feature**: 009-inventario-conteo  
**Discovered**: 2026-07-18  
**Status**: ✅ Patched (iteración 2026-07-18)

## Descripción del Problema

El flujo de Service.Iniciar() tiene el orden de operaciones INVERTIDO:

**ANTES (❌ Incorrecto)**:

```text
1. Crear registro en inventarios
   ↓
2. Intentar consultar items desde detalle_inventario (vacío)
   ↓
3. Retornar respuesta con 0 items
```

**DESPUÉS (✅ Correcto)**:

```text
1. Consultar items activos para tipo seleccionado
   ↓
2. Validar hay items (sino retornar 422)
   ↓
3. Crear registro en inventarios
   ↓
4. Cruzar items con stock_actual
   ↓
5. Crear detalles con valor_sugerido mapeado
   ↓
6. Retornar respuesta con N items
```

### Root Cause

El orden inverso viola RF-INV-02.3 "Determinación Automática de Items":

- Intenta crear detalles desde tabla vacía DESPUÉS de crear inventario
- No consulta items ANTES de validar
- No mapea valor_sugerido desde stock_actual

### Evidencia

```go
// repository.go línea 111
func (r *RepositoryImpl) CreateDetalleInventario(ctx context.Context, detalles []DetalleInventario) error {
    // Implementación lista
}

// Búsqueda en service.go: 0 resultados de CreateDetalleInventario
grep CreateDetalleInventario internal/inventarios/service.go
// (sin resultados)
```

## Flujo Que Debería Ocurrir

```text
POST /api/v1/inventarios
{
  "tienda_id": 1,
  "tipo": "diario",
  "horario": "apertura"
}

↓

Service.Iniciar():

1. ✓ Validar autorización (tienda)
2. ✓ Validar tipo y horario
3. ✓ Crear registro en inventarios
4. ❌ FALTA: Obtener todos los items ACTIVOS de la tienda
5. ❌ FALTA: Crear registros en detalle_inventario (uno por item)
6. ❌ FALTA: Tomar snapshot en stock_actual con valor_sugerido
7. ✓ Llamar GetInventarioDetalle() para retornar items

↓ Response (201 Created)
{
  "id": 123,
  "items": [
    { "item_id": 501, "valor_sugerido": 50 },
    { "item_id": 502, "valor_sugerido": 45 },
    ...
  ]
}
```

## Preguntas Abiertas

1. **¿De dónde obtiene los items?**
   - **Actual**: Intenta traer de `detalle_inventario` (vacío) → 0 items
   - **Debería**: De tabla `items` (con filtros: activos, tienda, frecuencia)

2. **¿Dónde viene `valor_sugerido`?**
   - **Actual**: No existe mecanismo
   - **Debería**: De `stock_actual.valor_snapshot` o default a 0

3. **¿Quién crea los registros en `detalle_inventario`?**
   - **Actual**: No se crean (GAP)
   - **Debería**: Service.Iniciar() → repo.CreateDetalleInventario()

4. **¿Se toma snapshot automáticamente?**
   - **Actual**: No (función existe pero no se llama)
   - **Debería**: Service.Iniciar() → repo.SnapshotStockActual()

## Impacto en Testing

El test script `test_inventario_conteo.sql` que creamos:

```sql
INSERT INTO items (codigo, nombre, ...) VALUES ('TEST-001', ...) -- 10 items
```

**NO funcionará** con el flujo actual porque:

1. Los 10 items se insertan en tabla `items` ✓
2. Pero cuando inicia conteo, busca en `detalle_inventario` ❌
3. Y `detalle_inventario` no tiene los items porque no se crean automáticamente ❌

**Resultado**: GET /inventarios/{id} retorna 0 items

## Solución Requerida

### Backend (service.go - método Iniciar)

```go
func (s *ServiceImpl) Iniciar(ctx context.Context, req *CreateInventarioReq, userID int64, role string, userTiendaID *int64) (*InventarioResp, error) {
    // ... validaciones existentes ...
    
    // Crear inventario
    createdInv, err := s.repo.CreateInventario(ctx, inv)
    if err != nil {
        return nil, err
    }
    
    // NUEVO: Obtener todos los items activos de la tienda
    // Filtros: activo=1, frecuencia_inventario=tipo (o todo)
    itemsActivos, err := s.repo.GetItemsActivosTienda(ctx, req.TiendaID)
    if err != nil {
        return nil, err
    }
    
    // NUEVO: Crear detalles para cada item
    detalles := make([]DetalleInventario, len(itemsActivos))
    itemIDs := make([]int64, len(itemsActivos))
    for i, item := range itemsActivos {
        detalles[i] = DetalleInventario{
            InventarioID: createdInv.ID,
            ItemID:       item.ID,
            ValorSugerido: 0, // O desde stock_actual
            ValorEsperado: 0,
        }
        itemIDs[i] = item.ID
    }
    
    err = s.repo.CreateDetalleInventario(ctx, detalles)
    if err != nil {
        return nil, err
    }
    
    // NUEVO: Tomar snapshot de stock actual
    err = s.repo.SnapshotStockActual(ctx, createdInv, itemIDs)
    if err != nil {
        s.logger.WarnContext(ctx, "inventario.iniciar: error tomando snapshot",
            "inventario_id", createdInv.ID)
        // No bloquear por RD-04 (requirement: snapshot es optional)
    }
    
    // Obtener detalles con valores sugeridos
    createdInv, err = s.repo.GetInventarioDetalle(ctx, createdInv.ID)
    if err != nil {
        return nil, err
    }
    
    return s.mapInventarioToResp(createdInv), nil
}
```

### Repository (repository.go - método nuevo)

```go
// GetItemsActivosTienda obtiene todos los items activos de una tienda
func (r *RepositoryImpl) GetItemsActivosTienda(ctx context.Context, tiendaID int64) ([]*Item, error) {
    query := `
        SELECT id, codigo, nombre, tipo, frecuencia_inventario, stock_seguridad, activo
        FROM items
        WHERE activo = 1
        ORDER BY id
    `
    
    rows, err := r.db.QueryContext(ctx, query)
    if err != nil {
        return nil, fmt.Errorf("error obteniendo items activos: %w", err)
    }
    defer rows.Close()
    
    items := []*Item{}
    for rows.Next() {
        item := &Item{}
        err := rows.Scan(
            &item.ID, &item.Codigo, &item.Nombre, &item.Tipo,
            &item.FrecuenciaInventario, &item.StockSeguridad, &item.Activo,
        )
        if err != nil {
            return nil, fmt.Errorf("error scanneando item: %w", err)
        }
        items = append(items, item)
    }
    
    return items, rows.Err()
}
```

## Diagrama Antes vs Después

### ANTES (Incorrecto)

```text
POST /inventarios
  └── Service.Iniciar()
      ├── CreateInventario() → OK, inventario_id = 123
      └── GetInventarioDetalle(123)
          ├── Query: SELECT * FROM detalle_inventario WHERE inventario_id = 123
          └── Resultado: 0 rows (vacío) → retorna 0 items
```

### DESPUÉS (Correcto — Patched)

```text
POST /inventarios
  └── Service.Iniciar()
      ├── GetItemsActivosPorTipo(tienda, tipo) → [item_501, item_502, ...]
      ├── Validar len(items) > 0 → SI: continuar, NO: retornar 422
      ├── CreateInventario() → OK, inventario_id = 123
      ├── GetStockSnapshot(tienda, items) → {item_501: 50, item_502: 45, ...}
      ├── CreateDetalleInventario(123, items, stocks) → 10 rows insertadas
      └── GetInventarioDetalle(123)
          ├── Query: SELECT * FROM detalle_inventario WHERE inventario_id = 123
          └── Resultado: 10 rows, cada una con valor_sugerido mapeado ✓
```

## Tareas de Corrección Planificadas

**Phase 12 (Nuevo)**: Tasks T154-T163 en tasks.md

| Task | Descripción |
|------|-------------|
| T154 | Implementar `GetItemsActivosPorTipo()` repository function |
| T155 | Implementar `GetStockSnapshot()` repository function |
| T156 | Refactorizar Service.Iniciar() - Paso 1: Query items ANTES |
| T157 | Refactorizar Service.Iniciar() - Paso 2: Cruzar con stock |
| T158 | Refactorizar Service.Iniciar() - Paso 3: Crear inventario + detalles |
| T159 | Unit test GetItemsActivosPorTipo() |
| T160 | Unit test GetStockSnapshot() |
| T161 | Integration test Service.Iniciar() completo |
| T162 | Handler mapping de error `sin_items_contabilizar` → 422 |
| T163 | Angular: Actualizar ErrorMapperService con nuevo código |

**Impacto en otros artifacts**:

- spec.md: ✅ Agregada RF-INV-02.3 + escenarios 5-6
- plan.md: ✅ Creado con fases y arquitectura
- data-model.md: ✅ Creado con flujo de datos correcto
- tasks.md: ✅ Reabiertos T021-T023, Agregadas T154-T163

## Backward Compatibility

**BREAKING CHANGE**: POST /inventarios ahora:

- **Siempre retorna** 201 con `items: [...]` (mínimo 1 item) O
- **Retorna** 422 `sin_items_contabilizar` si no hay items para tipo

Clientes esperando "0 items" en respuesta 201 se rompen. Frontend ya implementado está preparado.

## Validación

- ✅ Spec actualizada (RF-INV-02.3 explícito)
- ✅ Plan arquitectónico creado
- ✅ Data model con flujo 5-pasos documentado
- ✅ Tasks organizadas con dependencias claras
- ✅ Error code nuevo mapeado (422)
- ⏳ Implementación: Tasks T154-T163 pending

**Nota**: Iteración 2026-07-18 documenta la corrección. Implementación de T154-T163 es siguiente step.
